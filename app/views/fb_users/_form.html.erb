<%= form_with(model: fb_user, local: true) do |form| %>
  <% if fb_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fb_user.errors.count, "error") %> prohibited this fb_user from being saved:</h2>

      <ul>
        <% fb_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
